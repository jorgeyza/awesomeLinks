import { Box, Flex } from '@chakra-ui/react';
import { ChakraNextImage } from 'utils/ChakraNextImage';

const Navbar = () => {
  const lala = '';

  return <Flex>something</Flex>;
};

export default Navbar;
